---
layout: global
---
<article>
  <header>
    <h2>{{ title }}</h2>
    <p>Last updated: {{ page.date | date: "%m/%d/%Y" }}</p>
  </header>
  {{ content }}
</article>
<aside>
  {% if collections.projects %}
  <ul class="posts">
    {% for post in collections.projects %}
    <li class="post"><a href="{{ post.url }}">{{ post.data.title }}</a></li>
    {% endfor %}
  </ul>
  {% endif %}
</aside>