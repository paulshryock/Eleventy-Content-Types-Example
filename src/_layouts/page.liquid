---
layout: global
---
<article class="post">
  <header class="post__header">
    <h2 class="post__title">{{ title }}</h2>
    {% if description %}
    <p class="post__meta">{{ description}}</p>
    {% endif %}
  </header>
  {{ content }}
</article>